extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      //- title 
      h1 My Portfolio

      //- Add a description for your portfolio, what this project is, it's purpose, what it's supposed to accomplish, etc. 
      p.lead.text-light
        | This portfolio is visual resume designed to let potential employeers learn about me, my work, interests, and skills.
        | Created with using Node, Express, and Pug. My biggest challenge was getting the custom errors work correctly 🤪
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

    each project in projects
      .cell
        a(href=`/projects/${project.id}`)
          .overlay
            h2= project.project_name  
          img.thumbnail(src=`${project.image_urls[0]}`)
      
